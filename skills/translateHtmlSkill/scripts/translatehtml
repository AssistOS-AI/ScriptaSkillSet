#!/bin/sh
set -eu
SKILL_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if ! command -v node >/dev/null 2>&1; then
  echo "translatehtml: Node.js >=22 is required. See $SKILL_DIR/dependencies.md." >&2
  exit 1
fi
exec node "$SKILL_DIR/scripts/translatehtml.mjs" "$@"
